apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zbialik-selfsigned-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: zbialik.com
  secretName: zbialik-root-cert
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
