apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: test-kargo-promotion
spec:
  subscriptions:
  - chart:
      name: test-kargo-promotion
      registryURL: https://zbialik.github.io/helm-charts
      semverConstraint: ^0.1.4 # most-recent stable 0.x.x version (but less than 1.0.0) 
